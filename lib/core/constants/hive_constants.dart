/// Hive box names and keys used across the app.
class HiveConstants {
  HiveConstants._();

  static const String conversationsBox = 'conversations';
  static const String settingsBox = 'settings';

  /// Single key used to store app settings inside the settings box.
  static const String settingsKey = 'app_settings';
}
